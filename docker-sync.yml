version: "2"

options:
  verbose: true
syncs:
  churnometer-www-sync: # tip: add -sync and you keep consistent names as a convention
    src: '.'
    # sync_strategy: 'native_osx' # not needed, this is the default now
    # sync_excludes: ['ignored_folder', '.ignored_dot_folder']
